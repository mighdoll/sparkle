sparkle-time-server { // TODO rename to sparkle
    
  logging {
    provider = logback
    
    levels {
      root = DEBUG
    }
    
    file {
      path = "/tmp/sparkle-http-tests.log"
    }
  }

}
